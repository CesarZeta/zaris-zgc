# Copiar a .env.local (dev) o configurar como variables en Railway (prod).
# Seleccionar archivo con: $env:ENV_FILE=".env.local" (convencion ZGE)

ENV=dev
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/zgc_dev
JWT_SECRET=cambiar-por-secreto-largo-aleatorio
JWT_EXPIRES_HOURS=24
CORS_ORIGINS=http://localhost:5173,http://localhost:8080
