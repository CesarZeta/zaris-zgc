# Historial de Migraciones — ZGC

| # | Archivo | Local (zgc_dev) | Supabase (zaris-zgc) | Notas |
|---|---|---|---|---|
| 001 | `sql/001_nucleo_tenants_usuarios.sql` | ✅ 2026-07-03 | ✅ 2026-07-04 | tenants, sucursales, usuarios. RLS se agrega al desplegar en Supabase. |
| 002 | `sql/002_bue_entidades_clientes.sql` | ✅ 2026-07-03 | ✅ 2026-07-04 | BUE: entidades + contactos, provincias (códigos ARCA), zonas, condiciones_venta, rol clientes. |
| 003 | `sql/003_articulos_stock.sql` | ✅ 2026-07-04 | ✅ 2026-07-04 | Fase 2: familias/subfamilias, marcas, unidades, depositos, cotizaciones (USD), articulos (4 listas + flags POS), articulo_stock (saldos), stock_movimientos (kardex). |
| 004 | `sql/004_rubros_variantes.sql` | ✅ 2026-07-04 | ✅ 2026-07-04 | Fase 2.5: tenants.rubro, atributos + atributo_valores, articulo_variantes (EAN y dif_precio propios), variante_id nullable en articulo_stock y stock_movimientos (unique NULLS NOT DISTINCT). |
| 005 | `sql/005_rls_supabase.sql` | ✅ 2026-07-04 | ✅ 2026-07-04 | Hardening deploy: RLS deny-all en todo public + revoke a anon/authenticated (PostgREST cerrado; el backend, como dueño, bypassa). Idempotente; en local no hace nada visible. |
| 006 | `sql/006_ventas_comprobantes.sql` | ✅ 2026-07-04 | ✅ 2026-07-04 | Fase 3: puntos_venta, tipos_comprobante (seed 12 tipos), numeracion, comprobantes + items + alicuotas (patrón TABLAIVA→AlicIva) + vencimientos, recibos + medios, imputaciones, arca_config + arca_tokens. Extiende CHECK de stock_movimientos.tipo (devolucion/remito/anulacion). RLS enable en las 12 tablas nuevas. En Supabase la corrió César por SQL Editor (el MCP/psql no tenían credencial) y re-aplicó la 005 (idempotente) después. |
| 007 | `sql/007_compras_proveedores.sql` | ✅ 2026-07-05 | ✅ 2026-07-05 | Fase 4: proveedores (rol BUE), articulo_proveedores (comparativo), tipos_comprobante_compra (seed 10), compras + compra_items + compra_vencimientos, numeracion_compras (OP), ordenes_pago + medios, imputaciones_compras, articulos.proveedor_habitual_id. Sin cambios al CHECK del kardex (reusa compra/remito/devolucion/anulacion). RLS enable en las 10 tablas nuevas. En Supabase la corrió César por SQL Editor + re-aplicó la 005; smoke E2E contra prod OK. |
| 008 | `sql/008_caja_iva.sql` | ✅ 2026-07-05 | ✅ 2026-07-05 | Fase 5: conceptos_caja, caja_movimientos (manuales, tipo sellado), caja_cierres (arqueo, unique fecha/sucursal y global), retenciones (sufrida/practicada con CHECK cruzado), codigo_arca en tipos_comprobante_compra (libro/CITI, UPDATE 9). Libros de IVA y planilla diaria son reportes, sin tabla. RLS enable en las 4 tablas nuevas. Aplicada por psql via session pooler (:5432) + re-aplicada la 005 (idempotente); RLS verificado en las 4 tablas. |
| 009 | `sql/009_pos_mostrador.sql` | ✅ 2026-07-05 | ✅ 2026-07-05 | Fase 6: pos_cajas (config por caja: PV/depósito/lista/ancho ticket), pos_sesiones (turno de cajero con arqueo; unique parcial una abierta por caja), venta_medios (medios por venta contado, referenciando comprobantes y sesión) y comprobantes.pos_sesion_id. RLS enable en las 3 tablas nuevas. Aplicada por psql via session pooler (:5432) en single-transaction + re-aplicada la 005 (idempotente); RLS=t verificado en pos_cajas/pos_sesiones/venta_medios y `GET /pos/cajas` en prod → 200. |
| 010 | `sql/010_usuarios_roles_permisos.sql` | ✅ 2026-07-05 | ✅ 2026-07-05 | Fase 6.5 RBAC: roles (5 base por tenant, es_sistema), rol_permisos (nivel máximo por (rol, módulo); anular⇒editar⇒ver), usuarios.rol_id + backfill (usuarios existentes → admin; NULL = acceso total compat scripts). Seed idempotente para tenants existentes (los nuevos siembran lazy en GET /roles). RLS enable en las 2 tablas nuevas. En prod: psql via session pooler ANTES del push (el backend nuevo mapea usuarios.rol_id — sin la 010 se rompe el login), aprobado por César; + re-aplicada la 005. Verificado: 3 tenants × 5 roles, backfill de los 3 usuarios a admin, RLS=t, smoke 11/11 contra Vercel. |
| 011 | `sql/011_indices_performance.sql` | ✅ 2026-07-06 | ✅ 2026-07-06 | LOTE TÉCNICO: solo índices (`CREATE INDEX IF NOT EXISTS` + `CREATE EXTENSION pg_trgm`) — segura de aplicar sola, no cambia el modelo (el backend viejo y el nuevo funcionan con o sin ella). FKs de hijos selectin (comprobante_items, compra_items, recibo_medios, orden_pago_medios), GIN trgm multicolumna en entidades y articulos (los OR de búsqueda necesitan BitmapOr con índice en todas las ramas), (tenant_id, fecha) en recibos/ordenes_pago, parcial comprobantes(comprobante_asociado_id). Verificada en dev con EXPLAIN ANALYZE (BitmapOr, 0,9 ms sobre 12.208 artículos). En prod: psql via session pooler 2026-07-06 (aprobado por César), 9 índices + pg_trgm verificados por pg_indexes/pg_extension. |
| 012 | `sql/012_geo_domicilios_sucursales.sql` | ✅ 2026-07-06 | ✅ 2026-07-06 | Fase 7: aditiva e idempotente (solo `ADD COLUMN` nullable + tabla nueva). `entidades` += latitud/longitud; `sucursales` += provincia_id/codigo_postal/latitud/longitud (la 001 solo tenía domicilio/localidad); `tenants` += geo_centro_lat/lon/delta (sesgo Nominatim opcional); tabla nueva `entidad_domicilios` (tipo fiscal/entrega/otro, snapshot-ready para Logística F12-bis) con RLS enable. Segura de aplicar sola: el backend viejo funciona sin ella (solo el nuevo lee las columnas). En prod: la corrió César por psql session pooler `:5432` (con flags `-h/-p/-U/-d` que matchean el pgpass, no URL) + re-aplicó la 005; smoke E2E 14/14 verificó que entidades expone latitud. |
| 013 | `sql/013_cheques_bancos.sql` | ✅ 2026-07-06 | ✅ 2026-07-06 | Fase 8 (fila registrada retroactivamente el 2026-07-10 — la sesión de F8 no la anotó): greenfield cuentas_bancarias, cheques (máquina de estados), cheque_eventos (bitácora), extracto_imports, banco_movimientos (FK circular cheques↔banco_movimientos por ALTER al final) + seed del módulo de permisos `bancos` (admin/gerente anular, cajero editar, consulta ver) + RLS enable en las 5 tablas. En prod: aplicada por Claude vía psql session pooler `-w` (pgpass) ANTES del push; 5 tablas + RLS + 12 filas de permiso verificadas; smoke 12/12. |
| 014 | `sql/014_contabilizabilidad.sql` | ✅ 2026-07-10 | ✅ 2026-07-10 | Mini-fase CONTABILIZABILIDAD (pre-F9, ver docs/DISENO-CONTABILIDAD.md): aditiva e idempotente. `anulado_at`/`anulado_por` en recibos, imputaciones, ordenes_pago, compras, imputaciones_compras, caja_movimientos, retenciones, caja_cierres y banco_movimientos (anular = marcar, nunca borrar); uniques de caja_cierres → parciales `WHERE anulado_at IS NULL` (re-cierre de fecha reabierta); `recibos.rechazado_total` (el rechazo de cheque ya no reescribe total); `stock_movimientos.costo_unitario` (kardex valorizable); `cuenta_bancaria_id` en recibo_medios/orden_pago_medios/venta_medios/caja_movimientos; tabla nueva `compra_medios` (compras contado de gestión) con RLS + índice por FK; `cuentas_bancarias.saldo_inicial_fecha`. OJO deploy: el backend nuevo mapea estas columnas → la 014 va ANTES del push (como la 010). |

| 015 | `sql/015_contabilidad.sql` | ✅ 2026-07-11 | ✅ 2026-07-11 | Fase 9 Contabilidad (greenfield): plan_cuentas (jerárquico), asiento_mapeos (unique NULLS NOT DISTINCT), asientos + asiento_lineas (CHECK debe XOR haber, ON DELETE CASCADE para la regeneración), contab_periodos (unique parcial WHERE anulado_at IS NULL) + seed del módulo de permisos `contabilidad` (admin/gerente anular, consulta ver) + RLS en las 5 tablas. El plan de cuentas y los mapeos default se siembran LAZY por tenant (GET /contabilidad/plan) — la migración no carga datos de plan. Los asientos derivados son artefactos REGENERABLES (el motor los borra y re-deriva por período); el contrato de inmutabilidad 014 aplica a los documentos fuente y a los asientos MANUALES (anular = marcar). |
| 016 | `sql/016_activos_fijos_apareo.sql` | ✅ 2026-07-11 | ✅ 2026-07-11 | F9-bis (bienes de uso + apareo, diseño en docs/DISENO-CONTABILIDAD.md §6): aditiva e idempotente. Tablas nuevas `activo_categorias` (catálogo con vida útil sugerida; seed LAZY junto al plan) y `activos_fijos` (alta/baja/anulación marcada; el alta NO deriva asiento — el motor deriva amortización lineal mensual y la baja) con RLS; `banco_movimientos.contrapartida_id` (self-FK, apareo simétrico de transferencias entre cuentas propias → UN asiento banco a banco sin cuenta puente). Sin permisos nuevos (contabilidad/bancos ya existen). Segura de aplicar sola (el backend viejo no la lee), pero va ANTES del push porque el backend nuevo mapea la columna de banco_movimientos. |
| 017 | `sql/017_vendedores_comisiones.sql` | ✅ 2026-07-11 | ✅ 2026-07-11 | F11 Vendedores y comisiones (diseño en docs/DISENO-VENDEDORES-COMISIONES.md): aditiva e idempotente. `vendedores` (rol BUE, % único + modalidad venta/cobranza — VIAJANTE.DBF), `vendedor_id` en clientes/comprobantes/recibos (espejo CVIAJ; índices parciales), `comision_liquidaciones` + `comision_liquidacion_items` (documento contabilizable: % y modalidad sellados, anulable marcada; CHECK comprobante XOR recibo) + seed del módulo de permisos `vendedores` (admin/gerente anular, consulta ver) + RLS en las 3 tablas. OJO deploy: el backend nuevo mapea `vendedor_id` en tablas YA mapeadas (comprobantes/recibos/clientes) → la 017 va ANTES del push (como la 010/014: sin ella se rompen los SELECT de ventas). |
| 018 | `sql/018_plan_tenant.sql` | ✅ 2026-07-11 | ✅ 2026-07-11 | F12-a POS standalone (diseño en docs/DISENO-POS-PERFILES.md §7): aditiva e idempotente, solo `tenants` — `plan varchar(20) default 'suite'` (CHECK suite/pos, espejo del catálogo PLANES en permisos.py) + rubros `carniceria`/`restaurante` al CHECK de rubro (drop+add del constraint, idempotente). Sin tablas nuevas → NO necesita re-aplicar la 005 (RLS de tenants ya existe). OJO deploy: el backend nuevo mapea `tenants.plan` en una tabla YA mapeada → la 018 va ANTES del push (sin ella se rompe el login: plan_del_tenant se consulta en toda guarda). Los tenants existentes quedan `suite` (comportamiento idéntico al actual). |

| 019 | `sql/019_pos_super_pesables.sql` | ✅ 2026-07-11 | ✅ 2026-07-11 | F12-b perfil súper (diseño en docs/DISENO-POS-PERFILES.md §1): aditiva e idempotente. `articulos.codigo_balanza` (PLU corto, unique parcial por tenant, guardado sin ceros a la izquierda) + tabla nueva `pos_balanza_config` (una fila por tenant: prefijo 20-29, valor peso/importe, dígitos del PLU — esquema Kretz P(2)+PLU+VALOR+DV=13) con RLS. El parsing de etiquetas es server-side en GET /pos/buscar. Tabla nueva ⇒ re-aplicar la 005 en prod. Segura de aplicar sola (el backend viejo no la lee) pero va ANTES del push (el backend nuevo mapea `codigo_balanza` en `articulos`, tabla YA mapeada — sin ella se rompen los SELECT del maestro). |
| 020 | `sql/020_despiece_transformacion.sql` | ✅ 2026-07-11 | ✅ 2026-07-11 | F12-c despiece/transformación (diseño §2): `transformacion` al CHECK de stock_movimientos.tipo (salida origen + N entradas atadas por grupo_id, merma explícita, costeo proporcional al VALOR); tablas nuevas `despiece_plantillas` + `despiece_plantilla_cortes` (% rendimiento sugerido + coef. de valor; el UNIQUE (plantilla_id, articulo_id) cubre el índice del selectin) con RLS. La transformación NO deriva asientos (neutra en valor; el motor F9 solo mira ajuste/inicial). Tablas nuevas ⇒ re-aplicar la 005 en prod. |
| 021 | `sql/021_pos_resto.sql` | ✅ 2026-07-11 | ✅ 2026-07-11 | F12-d POS Resto (diseño §3): `pos_cajas.perfil` (estandar/resto) + tablas nuevas `pos_salones`, `pos_mesas` (unique salón+número), `pos_comandas` (cuenta abierta de mesa o pedido delivery/takeaway; unique parcial UNA abierta por mesa; domicilio snapshot OSM; envio_estado; propina_pct; comprobante_id al cobrar) y `pos_comanda_items` (precio snapshot informativo, estado_cocina, índice por FK) con RLS. Mandato: las comandas NUNCA llegan a la gestión — solo la venta final vía emitir_core. Tablas nuevas ⇒ re-aplicar la 005 en prod. Va ANTES del push (el backend nuevo mapea `pos_cajas.perfil`, tabla YA mapeada). |
| 022 | `sql/022_diferidos_op_sucursal_padron_cache.sql` | ✅ 2026-07-11 | ✅ 2026-07-11 | Lote de diferidos (fila registrada retroactivamente el 2026-07-13 — la sesión del lote no la anotó): `ordenes_pago.sucursal_id` (diferido F5: la planilla por sucursal suma sus OP; NULL = solo global) + tabla nueva `padron_cache` (una fila por tenant+cuit, TTL 24 h en el backend, `modo` sellado) con RLS. Tabla nueva ⇒ re-aplicada la 005 en prod. Aplicada por Claude vía psql session pooler ANTES del push (commit 22b6bcd); smoke prod 17/17. |
| 023 | `sql/023_nodo_sucursal.sql` | ✅ 2026-07-12 | ✅ 2026-07-12 | F13-LAN N1 (fila registrada retroactivamente el 2026-07-13): `sucursal_nodos` (aparejamiento con token bcrypt una-vez, un nodo activo por sucursal, PV propio) + `sync_checkpoints` + triggers `zgc_touch_updated_at` en entidades/clientes/articulos/variantes (réplica incremental confiable) con RLS. Tablas nuevas ⇒ re-aplicada la 005 en prod. Aplicada ANTES del push (commit ea15d5b); smoke prod 12/12. |
| 024 | `sql/024_nodo_sincronizacion.sql` | ✅ 2026-07-13 | ✅ 2026-07-13 | F13-LAN N2 (fila registrada retroactivamente el 2026-07-13): `updated_at` + triggers touch en las tablas mutables que suben (recibos, pos_sesiones, imputaciones, numeracion) para el checkpoint de subida por keyset. Sin tablas nuevas ⇒ no re-aplica la 005. Aplicada ANTES del push (commit 31193e6); suite única 87/87 + smoke prod 15/15. |
| 025 | `sql/025_logistica_entregas.sql` | ✅ 2026-07-13 | ✅ 2026-07-13 | F12-bis Logística de entregas (diseño en docs/DISENO-LOGISTICA-Y-DOMICILIOS.md §2): tablas nuevas `transportistas` (rol BUE, patrón vendedores), `hojas_ruta` (numeración por tenant, abierta→en_reparto→cerrada, anulable marcada) y `entregas` (una ACTIVA por comprobante — unique parcial —, domicilio SNAPSHOT, pendiente→asignada→en_reparto→entregada/rechazada; reprogramada = terminal) + seed del módulo de permisos `logistica` (admin/gerente anular, consulta ver; espejo permisos.py) + RLS en las 3 tablas. El estado de entrega NO toca el circuito fiscal ni la cta. cte. Tablas nuevas ⇒ re-aplicar la 005 en prod. Segura de aplicar sola (el backend viejo no la lee), y va ANTES del push como siempre. |

| 026 | `sql/026_salida_documentos.sql` | ✅ 2026-07-13 | ✅ 2026-07-13 | F16 Salida de documentos (diseño en docs/DISENO-SALIDA-DOCUMENTOS.md): tablas nuevas `email_envios` (registro de emails salientes con `modo` SELLADO — patrón ARCA; `cuerpo` TEXT deferred en el ORM; en modo simulado el registro es la única evidencia) y `password_resets` (recuperación autoservicio: solo el hash sha256 del token, expira 1 h, un solo uso) con RLS. Sin módulo RBAC nuevo (bandeja = configuracion.ver; envío = ventas.editar; recuperar/restablecer públicos). Tablas nuevas ⇒ re-aplicar la 005 en prod. Segura de aplicar sola (el backend viejo no la lee), y va ANTES del push como siempre. |

| 027 | `sql/027_auditoria.sql` | ✅ 2026-07-14 | ✅ 2026-07-14 | F17 Auditoría de acciones (diseño en docs/DISENO-AUDITORIA.md): tabla nueva `audit_eventos` — registro INMUTABLE por construcción (la API no expone UPDATE/DELETE, sin `updated_at`) de escrituras de configuración y eventos de seguridad que no dejan documento (usuario snapshot por email, acción de catálogo — 22 en `services/auditoria.py` —, ref, `detalle` JSONB sin material sensible, IP) + índices (tenant, fecha) y (tenant, accion) + RLS. Sin módulo RBAC nuevo (consulta = configuracion.ver, precedente F16). Tabla nueva ⇒ re-aplicar la 005 en prod. Segura de aplicar sola (el backend viejo no la lee), y va ANTES del push como siempre. |

Seed dev: `backend/seed_auth.py` → tenant "Empresa Demo SRL" + sucursal "Casa Central" + `admin@zgc.dev` / `123456` (aplicado en local 2026-07-03).
