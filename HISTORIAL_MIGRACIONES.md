# Historial de Migraciones — ZGC

| # | Archivo | Local (zgc_dev) | Supabase (zaris-zgc) | Notas |
|---|---|---|---|---|
| 001 | `sql/001_nucleo_tenants_usuarios.sql` | ✅ 2026-07-03 | ✅ 2026-07-04 | tenants, sucursales, usuarios. RLS se agrega al desplegar en Supabase. |
| 002 | `sql/002_bue_entidades_clientes.sql` | ✅ 2026-07-03 | ✅ 2026-07-04 | BUE: entidades + contactos, provincias (códigos ARCA), zonas, condiciones_venta, rol clientes. |
| 003 | `sql/003_articulos_stock.sql` | ✅ 2026-07-04 | ✅ 2026-07-04 | Fase 2: familias/subfamilias, marcas, unidades, depositos, cotizaciones (USD), articulos (4 listas + flags POS), articulo_stock (saldos), stock_movimientos (kardex). |
| 004 | `sql/004_rubros_variantes.sql` | ✅ 2026-07-04 | ✅ 2026-07-04 | Fase 2.5: tenants.rubro, atributos + atributo_valores, articulo_variantes (EAN y dif_precio propios), variante_id nullable en articulo_stock y stock_movimientos (unique NULLS NOT DISTINCT). |
| 005 | `sql/005_rls_supabase.sql` | ✅ 2026-07-04 | ✅ 2026-07-04 | Hardening deploy: RLS deny-all en todo public + revoke a anon/authenticated (PostgREST cerrado; el backend, como dueño, bypassa). Idempotente; en local no hace nada visible. |
| 006 | `sql/006_ventas_comprobantes.sql` | ✅ 2026-07-04 | ✅ 2026-07-04 | Fase 3: puntos_venta, tipos_comprobante (seed 12 tipos), numeracion, comprobantes + items + alicuotas (patrón TABLAIVA→AlicIva) + vencimientos, recibos + medios, imputaciones, arca_config + arca_tokens. Extiende CHECK de stock_movimientos.tipo (devolucion/remito/anulacion). RLS enable en las 12 tablas nuevas. En Supabase la corrió César por SQL Editor (el MCP/psql no tenían credencial) y re-aplicó la 005 (idempotente) después. |
| 007 | `sql/007_compras_proveedores.sql` | ✅ 2026-07-05 | ✅ 2026-07-05 | Fase 4: proveedores (rol BUE), articulo_proveedores (comparativo), tipos_comprobante_compra (seed 10), compras + compra_items + compra_vencimientos, numeracion_compras (OP), ordenes_pago + medios, imputaciones_compras, articulos.proveedor_habitual_id. Sin cambios al CHECK del kardex (reusa compra/remito/devolucion/anulacion). RLS enable en las 10 tablas nuevas. En Supabase la corrió César por SQL Editor + re-aplicó la 005; smoke E2E contra prod OK. |
| 008 | `sql/008_caja_iva.sql` | ✅ 2026-07-05 | ✅ 2026-07-05 | Fase 5: conceptos_caja, caja_movimientos (manuales, tipo sellado), caja_cierres (arqueo, unique fecha/sucursal y global), retenciones (sufrida/practicada con CHECK cruzado), codigo_arca en tipos_comprobante_compra (libro/CITI, UPDATE 9). Libros de IVA y planilla diaria son reportes, sin tabla. RLS enable en las 4 tablas nuevas. Aplicada por psql via session pooler (:5432) + re-aplicada la 005 (idempotente); RLS verificado en las 4 tablas. |
| 009 | `sql/009_pos_mostrador.sql` | ✅ 2026-07-05 | ✅ 2026-07-05 | Fase 6: pos_cajas (config por caja: PV/depósito/lista/ancho ticket), pos_sesiones (turno de cajero con arqueo; unique parcial una abierta por caja), venta_medios (medios por venta contado, referenciando comprobantes y sesión) y comprobantes.pos_sesion_id. RLS enable en las 3 tablas nuevas. Aplicada por psql via session pooler (:5432) en single-transaction + re-aplicada la 005 (idempotente); RLS=t verificado en pos_cajas/pos_sesiones/venta_medios y `GET /pos/cajas` en prod → 200. |
| 010 | `sql/010_usuarios_roles_permisos.sql` | ✅ 2026-07-05 | ✅ 2026-07-05 | Fase 6.5 RBAC: roles (5 base por tenant, es_sistema), rol_permisos (nivel máximo por (rol, módulo); anular⇒editar⇒ver), usuarios.rol_id + backfill (usuarios existentes → admin; NULL = acceso total compat scripts). Seed idempotente para tenants existentes (los nuevos siembran lazy en GET /roles). RLS enable en las 2 tablas nuevas. En prod: psql via session pooler ANTES del push (el backend nuevo mapea usuarios.rol_id — sin la 010 se rompe el login), aprobado por César; + re-aplicada la 005. Verificado: 3 tenants × 5 roles, backfill de los 3 usuarios a admin, RLS=t, smoke 11/11 contra Vercel. |
| 011 | `sql/011_indices_performance.sql` | ✅ 2026-07-06 | ✅ 2026-07-06 | LOTE TÉCNICO: solo índices (`CREATE INDEX IF NOT EXISTS` + `CREATE EXTENSION pg_trgm`) — segura de aplicar sola, no cambia el modelo (el backend viejo y el nuevo funcionan con o sin ella). FKs de hijos selectin (comprobante_items, compra_items, recibo_medios, orden_pago_medios), GIN trgm multicolumna en entidades y articulos (los OR de búsqueda necesitan BitmapOr con índice en todas las ramas), (tenant_id, fecha) en recibos/ordenes_pago, parcial comprobantes(comprobante_asociado_id). Verificada en dev con EXPLAIN ANALYZE (BitmapOr, 0,9 ms sobre 12.208 artículos). En prod: psql via session pooler 2026-07-06 (aprobado por César), 9 índices + pg_trgm verificados por pg_indexes/pg_extension. |
| 012 | `sql/012_geo_domicilios_sucursales.sql` | ✅ 2026-07-06 | ✅ 2026-07-06 | Fase 7: aditiva e idempotente (solo `ADD COLUMN` nullable + tabla nueva). `entidades` += latitud/longitud; `sucursales` += provincia_id/codigo_postal/latitud/longitud (la 001 solo tenía domicilio/localidad); `tenants` += geo_centro_lat/lon/delta (sesgo Nominatim opcional); tabla nueva `entidad_domicilios` (tipo fiscal/entrega/otro, snapshot-ready para Logística F12-bis) con RLS enable. Segura de aplicar sola: el backend viejo funciona sin ella (solo el nuevo lee las columnas). En prod: la corrió César por psql session pooler `:5432` (con flags `-h/-p/-U/-d` que matchean el pgpass, no URL) + re-aplicó la 005; smoke E2E 14/14 verificó que entidades expone latitud. |
| 013 | `sql/013_cheques_bancos.sql` | ✅ 2026-07-06 | ✅ 2026-07-06 | Fase 8 (fila registrada retroactivamente el 2026-07-10 — la sesión de F8 no la anotó): greenfield cuentas_bancarias, cheques (máquina de estados), cheque_eventos (bitácora), extracto_imports, banco_movimientos (FK circular cheques↔banco_movimientos por ALTER al final) + seed del módulo de permisos `bancos` (admin/gerente anular, cajero editar, consulta ver) + RLS enable en las 5 tablas. En prod: aplicada por Claude vía psql session pooler `-w` (pgpass) ANTES del push; 5 tablas + RLS + 12 filas de permiso verificadas; smoke 12/12. |
| 014 | `sql/014_contabilizabilidad.sql` | ✅ 2026-07-10 | ✅ 2026-07-10 | Mini-fase CONTABILIZABILIDAD (pre-F9, ver docs/DISENO-CONTABILIDAD.md): aditiva e idempotente. `anulado_at`/`anulado_por` en recibos, imputaciones, ordenes_pago, compras, imputaciones_compras, caja_movimientos, retenciones, caja_cierres y banco_movimientos (anular = marcar, nunca borrar); uniques de caja_cierres → parciales `WHERE anulado_at IS NULL` (re-cierre de fecha reabierta); `recibos.rechazado_total` (el rechazo de cheque ya no reescribe total); `stock_movimientos.costo_unitario` (kardex valorizable); `cuenta_bancaria_id` en recibo_medios/orden_pago_medios/venta_medios/caja_movimientos; tabla nueva `compra_medios` (compras contado de gestión) con RLS + índice por FK; `cuentas_bancarias.saldo_inicial_fecha`. OJO deploy: el backend nuevo mapea estas columnas → la 014 va ANTES del push (como la 010). |

Seed dev: `backend/seed_auth.py` → tenant "Empresa Demo SRL" + sucursal "Casa Central" + `admin@zgc.dev` / `123456` (aplicado en local 2026-07-03).
